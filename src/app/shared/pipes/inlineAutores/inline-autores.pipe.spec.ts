import { InlineAutoresPipe } from './inline-autores.pipe';

describe('InlineAutoresPipe', () => {
  it('create an instance', () => {
    const pipe = new InlineAutoresPipe();
    expect(pipe).toBeTruthy();
  });
});
