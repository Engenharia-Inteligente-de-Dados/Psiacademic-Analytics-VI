import { TranstornoPipePipe } from './transtorno-pipe.pipe';

describe('TranstornoPipePipe', () => {
  it('create an instance', () => {
    const pipe = new TranstornoPipePipe();
    expect(pipe).toBeTruthy();
  });
});
